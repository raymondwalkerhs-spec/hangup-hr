const { isOnline, requireOnline, verifyBackendAccess, verifyGoogleSheetsAccess } = require("./connectivity");

module.exports = { isOnline, requireOnline, verifyBackendAccess, verifyGoogleSheetsAccess };
