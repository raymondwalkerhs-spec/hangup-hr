-- NO-OP placeholder. Clear logic moved to 20260914_clear_training_quarter_extra_days.sql
-- so it sorts AFTER the temporary fix (…_training_quarter_day_pay_fix) is never re-applied after clear.
-- Kept so already-applied environments stay consistent with migration history.
SELECT 1;
